#include <iostream>
using namespace std;
#include <vector>
int main(){
    vector<int> arr={1,2,2,1,1};
    vector<int> name;
    vector<int> frequency;
    int count=0;
    int n=sizeof(arr)/sizeof(int);
    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++){
            if(arr[i]==arr[j]&&i!=j){
               count++;
               arr.
            }
        }
    }
}